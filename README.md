# lami

Utility scripts for running Lami TFLite models.

## GPU delegate execution

Use `run_lami_gpu.py` to execute a Lami model with the TFLite GPU delegate via LiteRT:

```bash
python run_lami_gpu.py /path/to/model.tflite
```
